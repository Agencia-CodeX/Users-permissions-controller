# RODANDO BACKEND

```bash
# Instale as dependencias necessarias para rodar a aplicação
$ yarn

# Inicialize o container do banco de dados
$ docker-compose up -d

# Rode a aplicação
$ yarn dev

# O servidor irá utilizar a porta 3333 - acesse http://localhost:3333
```